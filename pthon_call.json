curl -X POST http://localhost:8080/compress \
  -H "Content-Type: application/json" \
  -d '{
        "job_name": "LOCAL_TEST",
        "source_dir": "/data/incoming",
        "target_dir": "/data/archive",
        "file_pattern": "*.txt"
      }'

ts = datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
zip_file = f"{tgt}/{name}_{ts}.zip"

